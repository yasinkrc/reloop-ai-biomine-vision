# --- Çekirdek web katmanı ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
aiofiles==24.1.0
# PostgreSQL sürücüsü (docker-compose'daki db servisi için; SQLite'ta gerekmez)
psycopg2-binary==2.9.10

# --- Görüntü / bilim ---
numpy==1.26.4
opencv-python-headless==4.10.0.84
scikit-image==0.24.0
scipy==1.13.1
Pillow==10.4.0
imageio==2.36.1
imageio-ffmpeg==0.5.1

# --- Yapay zeka katmanı ---
# torch CPU/CUDA sürümü kurulum betiği (scripts/kurulum.sh) tarafından
# platforma göre kurulur. Aşağıdaki sürüm çoğu ortamda çalışır.
torch==2.5.1
torchvision==0.20.1

# Omnipose (opsiyonel ama önerilir). Kurulamazsa sistem klasik
# watershed tabanlı segmentasyona düşer ve çalışmaya devam eder.
# omnipose==1.0.6

# --- Genom / CRISPR-Cas analizi + grafikler ---
biopython==1.88
matplotlib==3.11.1
pygenomeviz==1.7.0
pycirclize==1.10.1   # dairesel (Circos tarzı) genom haritaları
# CRISPRCasTyper — Cas gen tiplemesi (opsiyonel). Kurulu değilse sistem
# yerleşik CRT tarzı CRISPR bulucuya düşer. HMM veritabanı + hmmer/prodigal gerekir.
# cctyper==1.8.0

# --- Hücre takibi ---
# trackastra — transformer tabanlı hücre takibi (opsiyonel). Kurulu değilse
# sistem yerleşik IoU + Macar algoritması takibine düşer.
# trackastra==0.3.0

# --- Raporlama / dışa aktarma ---
reportlab==4.2.5
pandas==2.2.3

# --- Test ---
pytest==8.3.4
httpx==0.28.1

# --- Opsiyonel harici ikili araçlar (pip değil) ---
#   brew install skani hmmer prodigal mmseqs2 mummer libomp   (macOS)
#   apt  install skani hmmer prodigal mmseqs2 mummer           (Debian/Ubuntu)
# skani  : genom benzerliği (ANI) ile en yakın tür/suş (yoksa tür ataması atlanır)
# mmseqs : GenBank girdilerde karşılaştırmalı sinteni (protein RBH)
# mummer : FASTA girdilerde karşılaştırmalı sinteni (nükleotid, nucmer)
# hmmer + prodigal : CRISPRCasTyper (cctyper) için gereklidir
